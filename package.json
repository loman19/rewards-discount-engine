{
  "name": "rewards-engine",
  "version": "0.1.0",
  "description": "Serverless-style rewards & discount engine (Instant Bank Discount, No Cost EMI, Exchange Discount) with idempotent, replay-safe request handling.",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/app.js",
    "dev": "ts-node src/app.ts",
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.9",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.4",
    "ts-jest": "^29.1.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
